# =============================================================================
# Openclaw Railway Template - Environment Variables
# =============================================================================
# Copy this file to .env for local development
# For Railway deployment, set these in the Railway dashboard Variables section
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED VARIABLES
# -----------------------------------------------------------------------------

# Password to access the /setup wizard
# For Railway: Use ${{ secret() }} to generate a random secure password
# For local: Set any password you want
SETUP_PASSWORD=your-secure-password-here

# Directory where openclaw stores configuration and state
# MUST point to /data on Railway (volume mount) for persistence
OPENCLAW_STATE_DIR=/data/.openclaw

# Directory where openclaw stores workspace files
# MUST point to /data on Railway (volume mount) for persistence
OPENCLAW_WORKSPACE_DIR=/data/workspace

# -----------------------------------------------------------------------------
# RECOMMENDED VARIABLES
# -----------------------------------------------------------------------------

# Authentication token for the openclaw gateway and Control UI
# For Railway: Use ${{ secret() }} to generate a stable secret
# For local: Generate with: openssl rand -hex 32
# If not set, the wrapper will generate one (not ideal for production)
OPENCLAW_GATEWAY_TOKEN=

# -----------------------------------------------------------------------------
# OPTIONAL VARIABLES (Usually don't need to change)
# -----------------------------------------------------------------------------

# Port the wrapper server listens on
# Railway sets this automatically, usually to 8080
PORT=8080

# Internal port where the gateway listens (proxied by the wrapper)
# Default: 18789
INTERNAL_GATEWAY_PORT=18789

# Internal host for the gateway
# Default: 127.0.0.1
INTERNAL_GATEWAY_HOST=127.0.0.1

# Path to the openclaw configuration file
# Default: ${OPENCLAW_STATE_DIR}/openclaw.json
# Leave empty to use the default
OPENCLAW_CONFIG_PATH=

# Path to the openclaw CLI entry point
# Default: /openclaw/dist/entry.js
# Leave empty unless using a custom build
OPENCLAW_ENTRY=/openclaw/dist/entry.js

# Node.js executable to use
# Default: node
# Leave empty unless you need a specific node binary
OPENCLAW_NODE=node

# Enable debug logging for wrapper token management
# Set to "true" to see detailed token resolution and sync logs
# Default: not set (debug disabled)
OPENCLAW_TEMPLATE_DEBUG=

# Fallback model when disabling ClawRouter/OpenRouter and no backup exists
# Only needed if the original model was never explicitly set in config
# Example: anthropic/claude-sonnet-4-20250514
DEFAULT_MODEL=

# -----------------------------------------------------------------------------
# CLAWROUTER (Smart LLM Routing)
# -----------------------------------------------------------------------------

# Enable ClawRouter for multi-model routing (~78% cost savings)
# Set to "true" to enable, anything else to disable
# Default: not set (disabled)
USE_CLAWROUTER=

# EVM wallet private key (0x...) for USDC micropayments via ClawRouter
# Required when USE_CLAWROUTER=true
# Fund with USDC on Base L2 before enabling
BLOCKRUN_WALLET_KEY=

# -----------------------------------------------------------------------------
# OPENROUTER (Kimi K2 / Multi-Model via OpenRouter)
# -----------------------------------------------------------------------------

# Enable OpenRouter as the LLM provider
# Set to "true" to enable, anything else to disable
# NOTE: USE_CLAWROUTER takes precedence — if both are true, ClawRouter wins
# Default: not set (disabled)
USE_OPENROUTER=

# OpenRouter API key (sk-or-...)
# Required when USE_OPENROUTER=true
# Get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# OpenRouter model ID (uses OpenRouter model naming)
# Default: openrouter/moonshotai/kimi-k2
# Browse models at https://openrouter.ai/models
OPENROUTER_MODEL=

# -----------------------------------------------------------------------------
# MOON SKILL (DEX Research, Trading & Scanning)
# -----------------------------------------------------------------------------

# Helius API key for enhanced Solana RPC, DAS metadata, holder analysis, scanner
# Required for: moon meta, moon holders, moon history, moon scan
# Get yours at https://helius.dev
HELIUS_API_KEY=

# Bankr API key for managed wallet trading (bk_... format)
# Required for: moon wallet, moon buy, moon sell, moon positions
# Get yours at https://bankr.fyi
BANKR_API_KEY=

# Jupiter API key (optional, improves rate limits for quotes/search/safety)
# Get yours at https://station.jup.ag
JUPITER_API_KEY=

# Custom Solana RPC (auto-upgraded to Helius when HELIUS_API_KEY is set)
# Default: https://api.mainnet-beta.solana.com
SOLANA_RPC_URL=

# Custom Base RPC endpoint
# Default: https://mainnet.base.org
BASE_RPC_URL=

# -----------------------------------------------------------------------------
# RAILWAY-SPECIFIC VARIABLES (Set automatically by Railway)
# -----------------------------------------------------------------------------

# Git commit SHA - automatically set by Railway during deployment
# Do not set manually
# RAILWAY_GIT_COMMIT_SHA=

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# 1. For Railway deployment, you MUST have a Volume mounted at /data
# 2. Without the volume, configuration will be lost on every redeploy
# 3. After deployment, visit https://your-app.up.railway.app/setup to configure
# 4. The setup wizard will guide you through onboarding
# =============================================================================
